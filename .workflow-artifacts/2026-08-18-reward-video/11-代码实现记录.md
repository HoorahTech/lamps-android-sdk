# 激励视频可插拔实现记录

## 实际修改范围

- `sdk`：增加激励视频 Provider 协议、注册表、默认 WebView Ability、slot `appId` 解析，以及穿山甲、优量汇、汇川三个实现。
- `demo`：验证三个广告 SDK 的传递依赖和 Manifest 合并。
- 根工程：增加汇川与穿山甲 Maven 仓库。

## 实施决策

- H5 固定调用 `hra.ad.showRewardedVideo`，不传 `slotId`。
- 核心按服务端 slot 顺序选择第一个已安装 Provider 可处理的配置。
- 汇川初始化使用 `rewardAdSlots[].appId`，不复用 Lamps appId，也不硬编码 heroes 的 appKey。
- 三个平台都放在 `sdk` 模块，通过类名反射延迟注册；宿主排除某个平台传递依赖后，不加载对应 Provider。
- SDK 和 Demo 的 `minSdk` 调整为 24，以满足当前穿山甲 `7.6.1.2` 的最低版本要求。
- 个性化广告开关默认关闭；当前需求没有提供隐私开关映射。

## 与方案一致性

- 三个平台均完成最小加载、展示、激励、关闭、错误和 15 秒加载超时链路。
- 已保留 heroes 的关键 H5 状态名称，移除 pid 接口请求、多 DSP 竞价、上报和预加载缓存。
- 未引入其他广告平台模块；后续平台可复用 `RewardAdProvider`。

## 验证

- `./gradlew --no-daemon :sdk:assembleDebug :demo:compileDebugKotlin`：通过。
- Manifest 合并随 demo 编译通过。
- 未执行真机广告请求；需要有效 appId、slotId、OAID 和汇川后台配置验证。

## 未完成项

- 未实现个性化广告授权的宿主配置入口。
- 未实现广告监控链接和竞价上报，符合本次最小化范围。
- 未配置 Maven 发布任务；传递依赖排除语法已记录，仍需在实际发布后验证 POM。

## 后续修改：Channel SDK 初始化收口

- 在 `data/sdk/channel` 的 `TTSdkManager`、`YLHSdkManager`、`NoahSdkManager` 中分别实现平台初始化。
- 三个 Manager 统一接收广告平台专用的 `ThirdSdkInitCallback`，分别通过 `success()` 和 `fail(code, message)` 返回初始化结果，避免与 Lamps SDK 主初始化回调混用。
- 三个 Reward Provider 改为调用对应 Manager，移除 Provider 内重复的初始化配置。
- `:sdk:compileDebugKotlin` 与 `:demo:compileDebugKotlin` 验证通过。

## 后续修改：配置成功后的 SDK 初始化分发

- 新增 `data/sdk/init/ISdkInitProvider`，并实现穿山甲、优量汇、汇川三个 Provider。
- 新增 `SdkInitDispatcher`，按服务端 slot 配置选择已注册的 Provider。
- `AppInitDataLoader` 返回远端请求状态；远端配置成功后，`SdkRuntime` 等待已配置第三方 SDK 初始化完成再进入 Ready。
- Channel Manager 增加重复初始化保护，避免启动阶段初始化后在广告请求阶段再次初始化。
- `:sdk:assembleDebug` 与 `:demo:compileDebugKotlin` 验证通过。

## 后续修改：初始化 Provider 注册方式

- 移除 `SdkInitDispatcher` 中基于描述符和反射创建 Provider 的实现。
- Dispatcher 初始化时通过 `register()` 直接注册三个 `ISdkInitProvider`，并按实现类去重。
- 分发初始化前按 slot 的 `appId` 去重，避免同一广告 SDK 重复初始化。
- 任务构建与主线程分发从 `initSdk()` 拆分；单任务启动合并回 `dispatchTasks()`，通过轻量回调方法聚合完成状态，不保留额外 Coordinator 类型。
- `:sdk:compileDebugKotlin` 与 `:demo:compileDebugKotlin` 验证通过。

## 后续修改：Lamps 调试信息页

- 参考 heroes 调试工具的 debug/release 隔离方式增加 `LampsSdkTools`，但界面和数据由 Lamps 自己实现。
- debug 源集新增 `LampsSdkToolsActivity`，展示基础参数、Application、接口 Init Data、debug 状态以及 Lamps/穿山甲/优量汇/汇川初始化状态；release 保留同签名空实现。
- 移除误加的穿山甲 `ads-sdk-tools` 依赖。
- Demo 增加 Lamps SDK Tools 按钮。
- SDK 与 Demo 的 debug/release assemble 均验证通过。

## 后续修改：初始化阶段耗时

- 新增 `SdkInitMetrics`，同时记录墙钟开始/结束时间和基于 `elapsedRealtime` 的阶段耗时。
- 覆盖 Lamps 总初始化、OAID、缓存读取、远端 Init Data 请求、Init Data 总加载、第三方 SDK 分发，以及穿山甲/优量汇/汇川各自初始化。
- 阶段结果包含运行中、成功、失败、跳过、缓存命中和缓存未命中。
- Lamps SDK Tools 增加初始化阶段耗时区域，支持刷新查看运行中阶段。
- `:sdk:assembleDebug` 与 `:demo:compileDebugKotlin` 验证通过。

## 后续修改：Init Data 缓存优先策略

- 缓存解析且业务校验通过时，立即用于本次 SDK 初始化，并在工作线程异步请求远端刷新缓存。
- 缓存缺失、解析失败或业务校验失败时，同步等待远端数据；远端数据解析和业务校验均通过才算成功。
- 校验要求 token 非空、至少存在一个广告位，并且每个广告位的 appId、slotId 和渠道标识有效。
- 缓存命中后的远端刷新只更新缓存，不替换本次运行时配置，避免已初始化 SDK 与新 slot/appId 不一致。
- 无论有效数据来自缓存还是远端，都会继续执行第三方 SDK 初始化分发。

## 后续修改：统一激励视频加载入口

- `ISdkInitProvider` 增加 `loadReward()`；穿山甲、优量汇、汇川初始化 Provider 分别将广告位路由到对应 Channel Manager。
- 三个平台的加载、展示、15 秒超时及平台回调转换均收口到 `TTSdkManager`、`YLHSdkManager`、`NoahSdkManager`。
- `LampsSdk` 新增公开的 `loadReward(Activity, RewardAdCallback)`，按服务端广告位顺序选择首个受支持平台并自动加载、展示。
- `RewardAdCallback` 统一为 `onAdLoaded`、`onAdShown`、`onAdRewarded`、`onAdClosed`、`onAdError`。
- WebView 激励视频 Ability 改为复用 `LampsSdk.loadReward()`；移除重复的 Reward Provider、注册表及内置 Provider 安装实现。
- 实现路径为当前主工程 `sdk` 模块；工程内未发现 `AGENTS.md`，按已确认方案和主工程现有约束实施。
- `JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home" ./gradlew --no-daemon :sdk:compileDebugKotlin :demo:compileDebugKotlin`：通过。
- 未执行真机广告加载；各平台有效 appId/slotId、后台权限和奖励回调仍需真机验证。

## 后续修改：SDK 广告位并发竞价

- 参考 heroes `AdRewardVideoSdkV2Adapter` 新增独立的 `SdkRewardDispatcher`，不再由 `SdkInitDispatcher` 承担激励视频分发。
- 每个受支持的 `rewardAdSlots` 都并发发起 SDK 加载；每个任务通过成功、失败或 Manager 的 15 秒超时进入终态。
- 等待全部任务完成后，仅在成功广告中按价格选择最高者；价格相同时保持服务端 slot 原始顺序。
- 三个平台 Manager 将加载与展示拆开，加载成功后返回持有平台广告对象的 `RewardAdCandidate`，竞价完成后才调用胜出广告的 `show()`。
- 价格来源：汇川使用 `RewardedVideoAd.price`，优量汇使用 `RewardVideoAD.getECPM()`，穿山甲优先读取 `mediaExtraInfo` 的价格字段；平台未返回有效价格时回退到 slot 的可选 `price`。
- `RewardSlotResponse` 与解析器新增 `price`，默认值为 `0.0`，兼容旧缓存和旧接口数据；Lamps SDK Tools 同步展示该字段。
- 所有平台加载失败时统一回调 `ALL_SDK_LOAD_FAILED`；胜出后统一复用公开 `RewardAdCallback` 返回加载、展示、奖励、关闭和错误事件。
- `JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home" ./gradlew --no-daemon :sdk:compileDebugKotlin :demo:compileDebugKotlin`：通过。
- 未验证真实平台返回价格的单位和穿山甲 `mediaExtraInfo` 具体键值；服务端 slot `price` 与各 SDK 实时价格需保证使用相同单位。

## 后续修改：初始化数据实体化

- 新增初始化专用实体 `SdkInitData`，保存对应 `ISdkProvider`、原始 `RewardSlotResponse`、初始化状态、错误码/错误信息、起止时间和耗时。
- 初始化状态统一为 `PENDING`、`INITIALIZING`、`SUCCESS`、`FAILED`，状态转换采用同步保护，避免平台重复回调导致重复计数。
- `SdkInitDispatcher` 将服务端 `rewardAdSlots` 转换为 `SdkInitData` 列表，并按 Provider 实现类去重，确保同一平台 SDK 仅初始化一次。
- Dispatcher 持有当前初始化数据列表并通过 `getInitDataList()` 提供内部只读列表快照。
- 移除 `SdkInitTask`，初始化分发、指标记录和聚合回调均直接基于 `SdkInitData`。
- `JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home" ./gradlew --no-daemon :sdk:compileDebugKotlin :demo:compileDebugKotlin`：通过。

## 后续修改：激励视频状态实体与加载展示分离

- 新增 `SdkRewardData`，保存 slot 顺序、Provider、原始 slot、平台广告对象、实时价格、错误信息、奖励状态，以及加载和展示的起止时间与耗时。
- 状态覆盖 `PENDING`、`LOADING`、`LOAD_SUCCESS`、`LOAD_FAILED`、`SELECTED`、`SHOWING`、`SHOWN`、`SHOW_FAILED`、`CLOSED`。
- `SdkRewardDispatcher` 持有 `SdkRewardData` 列表并通过 `getRewardDataList()` 提供内部只读快照，取消 `RewardLoadTask`、`LoadedReward` 和 `LoadFailure` 等任务型临时实体。
- `loadReward()` 只负责并发加载、等待全部广告位结束、完成竞价并缓存最高价广告，不再自动展示。
- 新增 `showReward()`，仅展示最近一次加载竞价胜出的广告；未加载时返回 `AD_NOT_LOADED`。
- 公开回调拆分为 `RewardAdLoadCallback` 和 `RewardAdShowCallback`；平台内部加载回调独立命名为 `RewardAdCandidateLoadCallback`。
- WebView 单次 H5 调用内部按 `loadReward()` 成功后再调用 `showReward()`，保持原有 H5 协议行为。
- `JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home" ./gradlew --no-daemon :sdk:compileDebugKotlin :demo:compileDebugKotlin`：通过；`git diff --check` 通过。

## 后续修改：移除 RewardAdCandidate

- 删除 `RewardAdCandidate`，平台广告对象和价格在加载成功后直接写入 `SdkRewardData`。
- 平台内部加载回调调整为 `RewardAdSdkLoadCallback.onLoadSuccess(adData, price)`。
- `ISdkProvider` 增加 `showReward(activity, adData, callback)`；Dispatcher 使用胜出数据对应的 Provider 展示广告。
- 穿山甲 Manager 直接接收 `TTRewardVideoAd` 展示；优量汇和汇川 Manager 使用广告对象到展示回调的并发映射，支持同平台多个 slot 并发加载。
- `JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home" ./gradlew --no-daemon :sdk:compileDebugKotlin :demo:compileDebugKotlin`：通过。

## 后续修改：穿山甲 load/show 监听分离

- 新增 `TTRewardVideoAdListenerAdapter` 与 `TTRewardAdInteractionListenerAdapter`，未关心的回调默认空实现。
- `loadReward()` 只覆盖 `onError`、`onRewardVideoAdLoad`；`showReward()` 再调用 `setRewardAdInteractionListener`，只覆盖展示、关闭、播放失败和奖励回调。
- 去掉 `RewardSlotResponse.price` 残留调用，加载价格只从穿山甲 `mediaExtraInfo` 读取，取不到则为 `0.0`。
- `:sdk:compileDebugKotlin` 与 `:demo:compileDebugKotlin` 验证通过。

## 后续修改：优量汇 RewardVideoAD 包装

- 新增 `YLHRewardVideoAd` 包装优量汇 `RewardVideoAD`，对外提供 `loadAD(loadCallback)` 和 `showAD(activity, showCallback)`。
- 包装类内部持有同一份 SDK 监听：加载阶段只处理加载成功/失败，展示阶段只处理展示、奖励、关闭和展示错误。
- 删除 `adListeners` 映射和 `YLHRewardVideoADListenerProxy`；`YLHSdkManager` 只创建包装对象并转发。
- `SdkRewardData` 缓存包装对象，Provider 按 `YLHRewardVideoAd` 展示。
- `:sdk:compileDebugKotlin` 与 `:demo:compileDebugKotlin` 验证通过。

## 后续修改：三个平台激励视频包装类对齐

- 新增 `TTRewardVideoAd`、`NoahRewardVideoAd`，与 `YLHRewardVideoAd` 一样对外提供 `loadAD` / `showAD`。
- 穿山甲官方类通过 `PangleRewardVideoAd` 别名引用，避免与包装类同名冲突。
- 三个 Manager 只负责初始化检查、创建包装对象并转发；Provider 缓存并展示对应包装对象。
- `:sdk:compileDebugKotlin` 与 `:demo:compileDebugKotlin` 验证通过。

## 后续修改：激励视频包装基类与价格内聚

- 新增统一基类 `RewardVideoAd`，声明 `getPrice()`、`loadAD()`、`showAD()`。
- `YLHRewardVideoAd`、`TTRewardVideoAd`、`NoahRewardVideoAd` 均继承该基类，并各自实现价格读取：优量汇 `ecpm`、穿山甲 `mediaExtraInfo`、汇川 `RewardedVideoAd.price`。
- `RewardAdSdkLoadCallback.onLoadSuccess` 去掉 `price` 参数，改为回传 `RewardVideoAd`；竞价通过 `ad.getPrice()` 取值。
- `SdkRewardData.price` 改为从包装对象实时读取，不再单独存储价格。
- `:sdk:compileDebugKotlin` 与 `:demo:compileDebugKotlin` 验证通过。

## 后续修改：LampsRewardAd 对外协议

- `LampsRewardAd` 提升为公开类，放到 `com.lamps.sdk.reward`。
- 对外只暴露 `price`、`slotId`、`channelName`、`isValid` 和 `show(activity, callback)`。
- Provider、slot 原始对象、平台包装对象、状态机和 `mark*` 均保持 internal。
- `RewardAdLoadCallback.onAdLoadSuccess` 回传 `LampsRewardAd`；`LampsSdk.showReward(activity, ad, callback)` 委托给 `ad.show()`。
- 公开回调 `RewardAdLoadCallback` / `RewardAdShowCallback` 一并放到 `com.lamps.sdk.reward`。
- `:sdk:compileDebugKotlin` 与 `:demo:compileDebugKotlin` 验证通过。

## 后续修改：激励视频入口校验收口到 SdkRuntime

- `LampsSdk.loadReward` / `showReward` 只转发 `SdkRuntime`。
- ready、Activity 可用性、config 校验以及分发调用都放到 `SdkRuntime`。
- `LampsRewardAd.show()` 也走 `SdkRuntime.showReward()`，避免绕过校验。
- `:sdk:compileDebugKotlin` 验证通过。

## 后续修改：Lamps SDK Tools 展示激励视频计时

- `SdkRewardDispatcher` 提供 `getRewardDataList()` 只读快照。
- Tools 页新增「激励视频」区块，展示每个 `LampsRewardAd` 的渠道、slot、状态、价格、奖励标记，以及 load/show 起止时间和耗时。
- `:sdk:compileDebugKotlin` 验证通过。

## 后续修改：Tools 使用 SdkInitData 计时

- `SdkInitDispatcher` 提供 `getInitDataList()`；第三方 SDK 初始化起止时间和耗时改读 `SdkInitData`，不再按平台重复写入 `SdkInitMetrics`。
- Tools「初始化阶段耗时」保留总流程 metrics，并追加每个 `SdkInitData` 的 state、start/end/duration。
- `:sdk:compileDebugKotlin` 验证通过。

## 后续修改：Demo 增加激励视频按钮

- Demo 首页新增 `Show Reward Video` 按钮，点击后 `loadReward`，成功则立即 `show`。
- 按钮下方展示加载/展示状态；流程进行中禁用按钮，结束或失败后恢复。
- `:demo:compileDebugKotlin` 验证通过。

## 后续修改：监测链接宏替换

- 新增 `MonitorMacroReplacer` / `MonitorReporter`：客户端替换全部约定宏；Android 将服务端 `dm` 映射为 `rem`。
- `__REM_SIGN__` 对齐 heroes：query 七个 key 升序拼接后追加 `rewardSignKey`，MD5 小写 32 位；仅 pm / rem 计算。
- `__REQUEST_ID__` 每次 load 生成 32 位无横线 UUID；H5 `forward_source` 写入 `__FORWARD_SOURCE__`。
- 激励视频节点上报：加载 RM、竞胜 WM、曝光 PM、点击 CM、发奖 REM（`__ACTION__=30`）。
- `:sdk:compileDebugKotlin` 与 `:demo:compileDebugKotlin` 验证通过。

## 后续修改：MonitorReporter 按事件拆方法

- 删除 `MonitorEvent`；`MonitorReporter` 不再接收 `LampsRewardAd`，广告侧字段由调用方以 map 传入。
- 上报拆成 `reportRm` / `reportWm` / `reportPm` / `reportCm` / `reportDm`。RM、WM、PM、DM 传 `__PRICE__` / `__UNION_NAME__` / `__SLOTID__`；CM 在渠道点击回调中无广告字段，不传 map。
- `reportDm` 内部补 `__ACTION__=30`；PM / DM 继续计算 `__REM_SIGN__`。
- 验证：未执行 Gradle 编译（默认留待 `/verify`）。

## 后续修改：监测上报补动态宏

- `reportWm` / `reportPm` / `reportCm` / `reportDm`（以及 RM）在默认设备宏之外写入 `__REQUEST_ID__`、`__FORWARD_SOURCE__`、`__PRICE__`、`__UNION_NAME__`、`__SLOTID__`。
- 竞胜时先 `newRequestAfter(winner)` 再 `reportWm`，避免 WM 时尚无价格和广告位。
- 验证：未执行 Gradle 编译（默认留待 `/verify`）。

## 后续修改：竞价失败保留广告对象和错误信息

- 加载成功但未竞胜的 `LampsRewardAd` 进入 `BID_FAILED`，保留 `RewardVideoAd`，并写入 `errorCode=2011` 与 `errorMessage`。
- 加载失败也会把渠道 `RewardVideoAd` 挂到 `LampsRewardAd`，同时补充 SDK 返回的 `code` / `msg`。
- Tools 激励视频区块增加 `hasAdData`，便于确认落选/失败条目是否仍持有广告对象。
- 验证：未执行 Gradle 编译（默认留待 `/verify`）。

## 后续修改：RM 监测使用当前广告字段

- `reportRmSuccess` / `reportRmFail` 用传入的 `LampsRewardAd` 填充 `__PRICE__`、`__UNION_NAME__`、`__SLOTID__`，不再用全局竞胜字段。
- 验证：未执行 Gradle 编译（默认留待 `/verify`）。

## 后续修改：requestId / forwardSource 挂到每条广告

- `LampsRewardAd` 增加 `requestId`、`forwardSource`；同一次 `loadReward` 共用一个无横线 UUID 和传入的 `forwardSource`。
- `MonitorReporter` 去掉全局字段，RM / WM / PM / CM / DM 都从当前 `LampsRewardAd` 取宏。
- Tools 激励视频区块展示 `requestId`、`forwardSource`。
- 验证：未执行 Gradle 编译（默认留待 `/verify`）。

## 后续修改：PM / DM 计算 __REM_SIGN__

- `reportPm` / `reportDm` 在其它宏替换完成后，用 query 七个 key（缺失跳过）拼接 `rewardSignKey`，MD5 小写 32 位写入 `__REM_SIGN__`。
- 验证：未执行 Gradle 编译（默认留待 `/verify`）。

## 后续修改：DM 增加 __ACTION__

- `reportDm` 写入 `__ACTION__=30`（激励视频任务完成）。
- 验证：未执行 Gradle 编译（默认留待 `/verify`）。

## 后续修改：监测设备宏写入 SP

- 稳定字段首次读取后写入 `lamps_sdk`：UA、ANDROID_ID、OAID、APP_ID、PHONE_BRAND；进程内再走内存缓存。
- TS、NETWORK、SW/SH、IP 仍每次现取；MAC、IMEI 保持空串，OS / SDK_VERSION 为常量不落盘。
- 验证：未执行 Gradle 编译（默认留待 `/verify`）。

## 后续修改：设备宏缓存收口到 DeviceUtils

- 删除 `SdkPrefs`；`oaid` / `appId` / `phoneBrand` / `androidId` / `userAgent` 的内存 + SP 缓存都由 `DeviceUtils` 管理。
- `MonitorUtil.buildDefaultValues()` 只调用 `DeviceUtils`，不再直接读写 SP。
- 验证：未执行 Gradle 编译（默认留待 `/verify`）。

## 后续修改：初始化请求 version 改为 SDK 版本

- `AppInitRequest.version` 重命名为 `sdkVersion`，取值 `BuildConfig.SDK_VERSION`；请求 query 仍传 `version`。
- 验证：未执行 Gradle 编译（默认留待 `/verify`）。

## 后续修改：RM 增加填充结果宏

- `reportRmSuccess` 写入 `__IS_SUCCESS__=1`，`__CODE__` 为空。
- `reportRmFail` 写入 `__IS_SUCCESS__=0`，`__CODE__` 为广告无填充时的错误码。
- 验证：未执行 Gradle 编译（默认留待 `/verify`）。

## 后续修改：汇川竞价胜负回传

- 对齐 heroes `NoahSdkRewardBiddingReporter` 与 Noah `sendWinNotification` / `sendLossNotification`：竞价结束、曝光前必须上报。
- 加载成功的汇川广告，竞胜回传成交价；竞败回传胜出方价格（没有则回传自身出价），原因 `BiddingLossReason.LOW_PRICE`。
- 验证：未执行 Gradle 编译（默认留待 `/verify`）。

## 后续修改：IMEI / MAC 对齐虎扑广告

- `__IMEI__` 对齐 `HpDeviceInfoExt.getImeiOrNull()`：仅 Android 10 以下且已授予 `READ_PHONE_STATE` 时读取，并写入 SP。
- `__MAC__` 对齐 `HpDeviceInfoExt.getMac()`：Android 10 及以下读网卡 MAC；Android 11 及以上有定位权限时读路由 BSSID，否则为 `02:00:00:00:00:00`。MAC 只做进程内缓存，不落盘。
- SDK Manifest 增加 `ACCESS_WIFI_STATE`；敏感权限由宿主申请，SDK 仅运行时检查。
- 验证：未执行 Gradle 编译（默认留待 `/verify`）。

## 后续修改：调试工具拆到独立 module

- 新增 `:sdk-tools`，迁入 `LampsSdkTools`、`LampsSdkToolsActivity` 和工具页资源；从 `:sdk` 的 debug/release 源集移除同名实现。
- `:sdk` 增加 `LampsSdkDebug.buildInfo()`，供工具页读取内部初始化/激励状态，避免把 `internal` API 暴露给宿主。
- Demo 使用 `debugImplementation(project(":sdk-tools"))`；debug 绑定工具入口，release 隐藏按钮，main 源集不再直接依赖 `LampsSdkTools`。
- 验证：`:sdk:compileDebugKotlin`、`:sdk-tools:compileDebugKotlin`、`:demo:compileDebugKotlin`、`:demo:compileReleaseKotlin`。

## 后续修改：sdk-tools 接入三方调试工具

- `:sdk-tools` 增加穿山甲 `ads-sdk-tools:7.6.4.2-hupu`、优量汇 `union:tools:2.4`；汇川预览页使用 `noah` 包内 `AdTestActivity`，不另加依赖。
- Lamps 工具页增加三个入口，分别调用 `ToolsUtil.start`、`ToolsActivity`、`com.noah.sdk.dg.external.AdTestActivity`。
- 优量汇 tools 仍依赖 Support 库，工程打开 `android.enableJetifier=true`。
- 验证：`:sdk-tools:compileDebugKotlin`、`:demo:compileDebugKotlin`。

## 后续修改：对齐 HPWebview 发布到 Nexus

- HPWebview 使用旧 `maven` 插件 `uploadArchives`，仓库 `http://nexus.hupu.io/repository/hupu-android/`，账号 `kr`。
- Lamps 使用 AGP 8 的 `maven-publish`，发布到同一托管仓库的 HTTPS 地址 `https://nexus.hupu.io/repository/hupu-android/`；SNAPSHOT 走 `hupu-android-snapshot`。
- 坐标 `com.lamps:sdk` / `com.lamps:sdk-tools`，版本取 `gradle.properties` 的 `LAMPS_VERSION`，并同步 `BuildConfig.SDK_VERSION`。
- 验证：`:sdk:publishToMavenLocal`、`:sdk-tools:publishToMavenLocal`。

## 后续修改：rewardSignKey 不落日志和 debug 页

- 调试信息页不再输出 `rewardSignKey`。
- `SdkLog` 对 JSON / query 中的 `rewardSignKey` 打码后再输出，避免 init HTTP body 泄漏。
- `AppInitResponse` / `AppInitDataSnapshot` 的 `toString` 不包含密钥和原始响应。
- 签名计算仍内部使用该字段，不改变监测上报行为。

## 后续修改：调试页分组折叠

- `LampsSdkDebug.buildSections()` 按分组返回调试信息。
- 工具页点击分组标题展开/折叠；SDK 状态、SDK初始化入参、Application 默认展开，SDK初始化阶段耗时、接口 Init Data、激励视频默认折叠。
- 复制仍输出全部分组内容；onResume 刷新时保留当前展开状态。

## 后续修改：调试页 Monitor 上报记录

- 每次 RM/WM/PM/CM/DM 监测 GET 写入 `MonitorReportRecorder`，记录事件、最终 URL、responseCode 和错误。
- 调试页底部新增「Monitor 上报」分组，默认折叠；每条上报单独折叠，标题展示 event、responseCode 和时间，展开后看完整 URL。
- 最多保留 100 条，避免内存无限增长。
