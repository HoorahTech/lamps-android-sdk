# 代码实现记录

## 本次目标

收敛 SDK 对外接入面，以 `LampsSdk`、`LampsConfig`、`OaidProvider`、`InitCallback` 和 `GameCenterView` 作为稳定接入 API，并为 release AAR 增加 R8 混淆与压缩；同步重写面向外部开发者的 README。

## 实施范围

- 对外保留 `OaidProvider`，由宿主提供 OAID 读取逻辑并通过 `LampsConfig.Builder.setOaidProvider` 注入。
- 对外保留 `InitCallback`，由宿主通过 `LampsSdk.startAsync` 接收初始化成功或失败结果。
- 对外保留 `GameCenterView`，游戏中心 API 返回该类型；宿主在页面永久销毁时调用 `GameCenterView.destroy()`。
- core、pangle、ylh、noah release 构建启用 R8；移除各 library 的 sources JAR 发布配置。
- consumer rules 只保留稳定入口、Manifest 组件、H5 JavaScript bridge 和联盟模块所需的最小内部 ABI。
- README 改为面向外部开发者的接入文档，并明确生命周期、混淆和发布约束。

## 约束与决策

联盟模块作为独立 AAR 发布，编译期必须链接 core 的少量 provider、广告契约和数据模型，因此这些内部 ABI 需要保留在产物中；它们不属于稳定接入 API。未被跨模块依赖的实现允许 R8 压缩和重命名。

## 验证

- `./gradlew :demo:compileDebugKotlin`：通过。
- `./gradlew :core:assembleRelease :pangle:assembleRelease :ylh:assembleRelease :noah:assembleRelease`：首次因跨 AAR 内部 ABI keep 不完整失败，补齐规则后 core、pangle、ylh、noah release 均构建通过。
- release AAR 检查：未包含 `sources.jar`，包含 consumer ProGuard 规则。
- `git diff --check`：通过。

## 未完成项

未执行真实宿主应用安装、真机 WebView 页面加载和第三方广告 SDK 运行验证；本次仅验证编译、release R8 产物和静态发布边界。

## 后续修改记录

### 2026-08-25：新增 sdk module

- 实施范围：新增 `sdk` Android library module，并在 `settings.gradle.kts` 注册 `:sdk`。
- 依赖：`sdk` 默认通过 `api(project(":core"))` 依赖并暴露本地 `core` 模块的稳定 API 类型；未修改 Maven 坐标或远端依赖配置。
- 构建配置：沿用现有 library 模块的 compileSdk 34、minSdk 24、Java/Kotlin 1.8、release R8 和 `maven-publish` 配置，artifactId 为 `sdk`，namespace 为 `com.lamps.sdk.sdk`。
- 新增文件：`sdk/build.gradle.kts`、`sdk/src/main/AndroidManifest.xml`、`sdk/consumer-rules.pro`、`sdk/proguard-rules.pro`。
- 方案偏差：无；未新增业务代码、Manifest 组件或额外模块依赖。
- 验证：执行 `git diff --check` 与 `./gradlew :sdk:assembleRelease`；未执行全量主工程编译、运行或真机验证。
- 未完成项：当时新增的空模块尚未包含对外 API；本次后续迁移已补入 `LampsSdk` facade。

### 2026-08-25：迁移 LampsSdk 到 sdk module

- 实施范围：将 `LampsSdk` 源码从 `core` 移至 `sdk`，`sdk` 通过 `api(project(":core"))` 复用并暴露现有配置、回调和 View API 类型。
- 跨模块调整：将 `SdkRuntime` 调整为 module 间可见；core 内部调试和激励能力改为直接调用 `SdkRuntime`，消除对 sdk facade 的反向依赖。
- 构建与发布：在 sdk 中同步 `SDK_VERSION` BuildConfig 字段和 facade 混淆规则；从 core 混淆规则移除 `LampsSdk` 入口保留项。
- 使用方：demo 的本地 Lamps 依赖由 `:core` 切换为 `:sdk`，验证宿主通过 sdk 接入。
- 方案偏差：无额外业务 API 变更；`SdkRuntime` 的 public 可见性是跨 module 编译所需，属于实现边界调整。
- 未完成项：未执行真实宿主安装、真机和运行时初始化验证。
- R8 修正：补充 core 对跨 AAR 使用的 `SdkRuntime` keep 规则，避免 sdk release R8 将其识别为缺失类。
- 验证结果：`:sdk:compileReleaseKotlin`、`:demo:compileDebugKotlin`、`:demo:compileReleaseKotlin` 和 `:sdk:assembleRelease` 均通过；release AAR classes.jar 包含 `com/lamps/sdk/LampsSdk.class`。

### 2026-08-25：迁移 LampsConfig 到 sdk module

- 实施范围：将公开 `LampsConfig` 和 Builder 从 core 移至 sdk；`LampsSdk.init` 在 sdk 内将其转换为 core 的 `SdkConfig`。
- Core 状态：新增 `SdkConfig` 作为 core 配置快照与 `current` 持有者，core 业务和渠道模块统一改用该类型，不再反向依赖 sdk。
- 依赖约束：sdk 保持 `implementation(project(":core"))`，未改为 `api`，避免 core 对 sdk 形成反向依赖。
- 混淆：公开配置入口的 keep 规则移到 sdk；core 保留 `SdkConfig` 及其跨模块运行时所需规则。
- 方案偏差：无对外调用方式变化，宿主仍使用 `com.lamps.sdk.config.LampsConfig`。
- 验证结果：`:sdk:compileReleaseKotlin`、`:demo:compileDebugKotlin`、`:demo:compileReleaseKotlin` 和 `:sdk:assembleRelease` 均通过；release AAR classes.jar 包含 `LampsSdk.class` 与 `config/LampsConfig.class`。

### 2026-08-25：修复 sdk implementation 下 InitCallback 宿主不可见

- 原因：`InitCallback` 仍位于 core，sdk 使用 `implementation(:core)` 时宿主无法解析 `LampsSdk.startAsync` 的回调类型。
- 修复：将公开 `com.lamps.sdk.core.InitCallback` 移至 sdk；core 新增内部 `CoreInitCallback`，由 `LampsSdk.startAsync` 做适配转发。
- 兼容性：宿主代码和 import 路径不变，`LampsSdk.startAsync(object : InitCallback { ... })` 保持原调用方式。

### 2026-08-25：修复 sdk implementation 下 GameCenterView 宿主不可见

- 原因：`GameCenterView` 仍位于 core，宿主声明 `private var gameCenterView: GameCenterView?` 时无法解析 core 类型。
- 修复：将 View 实现迁移到 sdk；core 的 `SdkRuntime` 改为只返回游戏中心 URL，由 sdk facade 创建并返回 sdk 内的 `GameCenterView`。
- 兼容性：宿主仍使用 `com.lamps.sdk.view.GameCenterView` 和现有 `getGameCenterView()` 调用方式。
- R8 修正：补充 core 对跨 AAR 使用的 `LampsWebView` keep 规则，确保 sdk 中的 GameCenterView 运行时依赖完整。

### 2026-08-25：移除 demo 对 core 的显式依赖

- 实施范围：将公开 `OaidProvider` 迁移到 sdk，core 新增 `CoreOaidProvider` 并由 `LampsConfig` 转换适配。
- 依赖调整：移除 demo 的 `implementation(project(":core"))`；demo 仅通过 sdk 和渠道模块接入。
- 约束：sdk 仍保持 `implementation(project(":core"))`，未改为 `api`。

### 2026-08-25：重梳理各 module 混淆规则

- sdk：仅保留 `LampsSdk`、`LampsConfig`、`InitCallback`、`OaidProvider`、`GameCenterView` 公开入口。
- core：仅保留 sdk/渠道跨 AAR 所需的 runtime bridge、配置快照、WebView、provider 基类和精确 ABI；移除整包 `**` 保留。
- 渠道模块：consumer rules 只保留 Manifest provider 构造入口；本地 R8 规则只保留 provider 和精确 ABI。
- sdk-tools：新增独立 consumer/proguard 规则，仅保留工具入口和 Manifest Activity，并启用 release R8。
- 未暴露内容：core 内部业务实现、调试数据、网络/缓存/监控实现和渠道内部实现不再通过 keep 规则整体保留。
- R8 修正：为 sdk-tools 跨 AAR 使用的三个调试入口补充精确 keep，不恢复 debug 包整包保留。
- Provider 修正：Manifest provider 及 `LampsProvider` 基类改为类级精确保留，防止 R8 破坏跨 AAR 的 `createProvider()` 覆盖关系；未扩大到 provider 包。

### 2026-08-25：发布统一切换到 GitHub Actions

- 移除 Gradle `mavenLocal()` 仓库，避免本地 Maven 产物参与依赖解析。
- 移除 `gpr.user` / `gpr.key` 本地 Gradle 属性凭据入口；发布仓库仅读取 GitHub Actions 注入的 `GITHUB_ACTOR` / `GITHUB_TOKEN`（兼容 `GH_TOKEN`）。
- 保留 `maven-publish` 和 `publish` task，因为 GitHub Actions 通过 Gradle publish 任务发布 AAR。
- README 明确 AAR 发布统一由 GitHub Actions 执行。

### 2026-08-25：TrackSdk 改为压缩 JSON 上报

- 实施范围：`core` 的 `TrackSdk.sendData`。
- 请求体：设备公共字段（`buildDefaultValues`）和 `type` 放在 JSON 外层；调用方传入的 `data` 放入外层 `pdata` 对象。
- 传输：JSON UTF-8 gzip 压缩后使用 Android Base64 无换行编码，通过 `POST` 发送到原 `REPORT_PATH`（按当前 SDK API 环境拼接），请求头为 `Content-Type: application/json`。
- 方案偏差：按后续确认保留原有 `REPORT_PATH` 和环境地址拼接方式；未修改 `HttpUtils` 公共网络实现。
- 验证：`git diff --check` 与 `./gradlew --no-daemon :core:compileDebugKotlin` 通过；编译仅有既有弃用和未使用参数警告。

### 2026-08-25：发布仓库切换到虎扑 Nexus

- 实施范围：根 Gradle 发布仓库、各 library 的 release sources JAR、POM SCM 元数据、GitHub/GitLab CI 发布入口和 README。
- Maven 仓库：`https://nexus.hupu.io/repository/hupu-android-public/`；`com.lamps` group 和现有 artifactId 保持不变。
- SCM：POM 的项目 URL、connection 和 developerConnection 统一指向 `http://gitlab.hupu.com/HPBase/lamps-android-sdk.git`。
- CI：发布凭据使用 `HUPU_NEXUS_USERNAME` / `HUPU_NEXUS_PASSWORD`，只从 CI Secret 注入；GitLab tag `v*` 或手动 pipeline 执行发布。
- 验证：待执行 Gradle publish task 的配置检查；未执行真实 Nexus 上传。
